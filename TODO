Add PWA
Make server that scans + analyzes data (probably python, maybe something else)
Let scouts draw auto paths (maybe with p5.js)? Have a few points and lines inbetween so size of qr code is small 